@extends('app')

@section('content')

<div class="row">
    <div class="col-md-12">
        {!! Form::open(['route' => ['((resource)).update', $((resource))->id]], 'method' => 'post']) !!}
            @include('((resource)).partials.form', ['buttonText' => 'Update ((resource))'])
        {!! Form::close() !!}

        {{-- @include('errors.validation') --}}
    </div>
</div>

@stop
